<h1> Edit!</h1>

<%= form_for :user, url: user_path(@user), method: :patch do |f| %>

  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@user.errors.count, "error") %> prohibited
        this student from being saved:
      </h2>
      <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <strong><%= f.label 'Student\'s name:' %></strong><br>
    <%= f.text_field :name %>
  </p>



  <p>
    <strong><%= f.label 'Favourite colour:' %></strong><br>
    <%= f.text_field :email %>
  </p>




  <p>
    <%= f.submit %>
  </p>


<% end %>
